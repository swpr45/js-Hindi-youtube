"use strict"; //treat all JS code as newer version

// alert(3+3) //we are using nodjs,not browser hence this alert() funtion gives error here
console.log(3
    +
    3) //code readability should be high, this is good practice 
    // every statement should be on next line 
    console.log("Swapnil")
    let isLoggedIn = false
    let age=23
    let Sname='Swapnil Kuwar'
    let state;

    // Primitive DataTypes
    //number => 2 to power 53 range
    //bigint =>if value is beyond above range then use bigint
    //string => either "" or ''
    //boolean =>true /false
    //null => standalone value
    //udefined => not provided any value 
    //symbol => unique

    //object
    console.log(typeof undefined ); // type is underdefined

    console.log(typeof null); // type is object
